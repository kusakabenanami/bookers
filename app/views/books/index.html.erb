<h1>Books</h1>

<% @books.each do |book| %>
<table>
  <thead>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
    </tr>
  </thead>
</table>
<table>
  <thead>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
    </tr>
  </thead>
</table>
<% end %>


<!--新規入力フォーム-->
  <h2>New Book</h2>

<%= form_with model: @book, url: '/books', method: :post do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <br>
  <%= f.submit 'Create Book' %>
<% end %>
  </br>